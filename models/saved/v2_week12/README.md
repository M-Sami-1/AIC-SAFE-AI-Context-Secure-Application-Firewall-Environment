# v2_week12 Checkpoint

This directory is reserved for the full-dataset TF-IDF checkpoint.
It stores `classifier.joblib` for prompt safety and `intent_classifier.joblib` for tool intent.

Generate it with:

```powershell
scripts/setup_venv.ps1
scripts/run_reproducible.ps1
```
